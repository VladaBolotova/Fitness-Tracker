const submitFormHandler = async(event)=> {
    event.preventDefault();

    const date = document.querySelector('').value.trim();
    const duration = document.querySelector('').value.trim();

    const type = document.querySelector('').value.trim();
    const strengh = document.querySelector('').value.trim();